<h1>Vector Collection</h1>
<p class="lead">Browse our comprehensive collection of Pichia pastoris vectors for protein expression and genome engineering</p>

<!-- Toggle between sale and subscription vectors -->
<div class="d-flex justify-content-center mb-3">
  <div class="vector-type-toggle" role="group" aria-label="Vector type toggle">
    <%= link_to vectors_path(vector_type: 'sale', category: @category),
                class: "btn #{@vector_type == 'sale' ? 'btn-primary' : 'btn-outline-secondary'}" do %>
      <i class="bi bi-cart"></i> For Sale
    <% end %>
    <%= link_to vectors_path(vector_type: 'subscription', category: @category),
                class: "btn #{@vector_type == 'subscription' ? 'btn-primary' : 'btn-outline-secondary'}" do %>
      <i class="bi bi-arrow-repeat"></i> Subscription (Twist)
    <% end %>
  </div>
</div>

<!-- Toggle between categories -->
<div class="d-flex justify-content-center mb-4">
  <div class="vector-type-toggle" role="group" aria-label="Category toggle">
    <%= link_to vectors_path(vector_type: @vector_type, category: 'expression'),
                class: "btn #{@category == 'expression' ? 'btn-primary' : 'btn-outline-secondary'}" do %>
      <i class="bi bi-cpu"></i> Protein Expression
    <% end %>
    <%= link_to vectors_path(vector_type: @vector_type, category: 'engineering'),
                class: "btn #{@category == 'engineering' ? 'btn-primary' : 'btn-outline-secondary'}" do %>
      <i class="bi bi-tools"></i> Genome Engineering
    <% end %>
  </div>
</div>

<% if @vectors.empty? %>
  <div class="alert alert-info" role="alert">
    <% if @category == 'expression' %>
      <% if @vector_type == 'subscription' %>
        No protein expression vectors are currently available through Twist subscription. Please check back later or try genome engineering vectors.
      <% else %>
        No protein expression vectors are currently available for sale. Please check back later or try genome engineering vectors.
      <% end %>
    <% else %>
      <% if @vector_type == 'subscription' %>
        No genome engineering vectors are currently available through Twist subscription. Please check back later or try protein expression vectors.
      <% else %>
        No genome engineering vectors are currently available for sale. Please check back later or try protein expression vectors.
      <% end %>
    <% end %>
  </div>
<% else %>
  <% @vectors_by_promoter.each do |promoter, vectors| %>
    <% if vectors.any? %>
      <section class="mb-5">
        <div class="d-flex align-items-center mb-4">
          <% if promoter.nil? %>
            <i class="bi bi-dash-circle fs-2 text-muted me-3"></i>
          <% elsif promoter.name.downcase == "none" %>
            <i class="bi bi-dash-circle fs-2 text-muted me-3"></i>
          <% elsif promoter.inducible? %>
            <i class="bi bi-lightning fs-2 text-warning me-3"></i>
          <% else %>
            <i class="bi bi-play-fill fs-2 text-primary me-3"></i>
          <% end %>
          <div>
            <h2 class="mb-1">
              <%= promoter.nil? ? "No Promoter" : "#{promoter.full_name} (#{promoter.name})" %>
            </h2>
            <p class="text-muted mb-0">
              <% if promoter.nil? %>
                Vectors without a specific promoter
              <% elsif promoter.name.downcase == "none" %>
                Vectors without a promoter sequence
              <% elsif promoter.inducible? %>
                Inducible promoter - <%= promoter.strength.downcase %> expression when induced
              <% else %>
                Constitutive promoter - <%= promoter.strength.downcase %> expression
              <% end %>
              <% if vectors.count == 1 %>
                · 1 vector
              <% else %>
                · <%= vectors.count %> vectors
              <% end %>
            </p>
          </div>
        </div>

        <div class="row">
          <% vectors.each do |vector| %>
            <div class="col-md-6 col-lg-4 mb-4">
              <div class="card bio-card h-100">
                <% if vector.map_image_exists? && vector.map_thumbnail %>
                  <%= link_to vector_path(vector) do %>
                    <% begin %>
                      <%= image_tag vector.map_thumbnail, 
                                    class: "card-img-top",
                                    style: "height: 200px; object-fit: cover; max-width: 100%;",
                                    alt: "#{vector.name} vector map",
                                    loading: "lazy" %>
                    <% rescue => e %>
                      <div class="card-img-top d-flex align-items-center justify-content-center bg-light" style="height: 200px;">
                        <div class="text-center text-muted">
                          <i class="bi bi-image fs-1"></i>
                          <p class="small mb-0">Image unavailable</p>
                        </div>
                      </div>
                    <% end %>
                  <% end %>
                <% end %>
                <div class="card-body d-flex flex-column">
                  <div class="d-flex align-items-start justify-content-between mb-2">
                    <h5 class="card-title mb-0"><%= vector.name %></h5>
                    <% if vector.category.present? %>
                      <% if vector.category == 'Genome Engineering' %>
                        <span class="badge bg-success">Engineering</span>
                      <% elsif vector.category == 'Heterologous Protein Expression' %>
                        <span class="badge bg-primary">Expression</span>
                      <% end %>
                    <% end %>
                  </div>

                  <p class="card-text flex-grow-1"><%= truncate(vector.description, length: 120) %></p>

                  <div class="mb-3">
                    <small class="text-muted">
                      <strong>Selection:</strong> <%= vector.selection_marker&.name || "N/A" %><br>
                      <strong>Type:</strong> <%= vector.vector_type&.name || "N/A" %><br>
                      <strong>Size:</strong>
                      <% if vector.vector_size %>
                        <span class="measurement"><%= number_with_delimiter(vector.vector_size) %><span class="unit">bp</span></span>
                      <% else %>
                        N/A
                      <% end %>
                      <% if vector.has_lox_sites? %>
                        <br><strong>Features:</strong> <span class="badge bg-warning text-dark">Lox sites</span>
                      <% end %>
                    </small>
                  </div>
                  
                  <div class="d-flex justify-content-between align-items-center mt-auto">
                    <div>
                      <% if @vector_type == 'subscription' %>
                        <% if vector.subscription_price %>
                          <strong class="text-bio-success measurement">$<%= number_with_precision(vector.subscription_price, precision: 0) %></strong>
                          <small class="text-muted d-block">per subscription</small>
                        <% else %>
                          <small class="text-muted">Contact for pricing</small>
                        <% end %>
                      <% else %>
                        <% if vector.sale_price %>
                          <strong class="text-bio-success measurement">$<%= number_with_precision(vector.sale_price, precision: 0) %></strong>
                        <% else %>
                          <small class="text-muted">Contact for pricing</small>
                        <% end %>
                      <% end %>
                    </div>
                    <%= link_to "View Details", vector, class: "btn btn-sm btn-primary" %>
                  </div>
                </div>
              </div>
            </div>
          <% end %>
        </div>
      </section>
    <% end %>
  <% end %>
<% end %>
