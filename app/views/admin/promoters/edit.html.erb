<div class="max-w-2xl mx-auto">
  <div class="bg-white shadow-sm rounded-lg">
    <div class="px-6 py-4 border-b border-gray-200">
      <h2 class="text-xl font-semibold text-gray-900">Edit Promoter</h2>
    </div>
    <div class="px-6 py-6">
      <%= form_with model: [:admin, @promoter], local: true do |form| %>
        <% if @promoter.errors.any? %>
          <div class="bg-red-50 border border-red-200 rounded-md p-4 mb-6">
            <div class="flex">
              <div class="flex-shrink-0">
                <svg class="h-5 w-5 text-red-400" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
                  <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd" />
                </svg>
              </div>
              <div class="ml-3">
                <h3 class="text-sm font-medium text-red-800 mb-2">Please fix the following errors:</h3>
                <ul class="list-disc list-inside text-sm text-red-700">
                  <% @promoter.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                  <% end %>
                </ul>
              </div>
            </div>
          </div>
        <% end %>

        <div class="mb-4">
          <%= form.label :name, "Promoter Name", class: "block text-sm font-medium text-gray-700 mb-1" %>
          <%= form.text_field :name, class: "block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm", required: true %>
          <p class="mt-1 text-sm text-gray-500">Short identifier for the promoter</p>
        </div>

        <div class="mb-4">
          <%= form.label :full_name, "Full Name", class: "block text-sm font-medium text-gray-700 mb-1" %>
          <%= form.text_field :full_name, class: "block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm" %>
          <p class="mt-1 text-sm text-gray-500">Complete scientific name (optional)</p>
        </div>

        <div class="mb-4">
          <div class="flex items-start">
            <div class="flex items-center h-5">
              <%= form.check_box :inducible, class: "h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 rounded" %>
            </div>
            <div class="ml-3">
              <%= form.label :inducible, "Inducible promoter", class: "text-sm font-medium text-gray-700" %>
              <p class="text-sm text-gray-500">Check if this promoter is inducible (requires activation)</p>
            </div>
          </div>
        </div>

        <div class="mb-6">
          <%= form.label :strength, "Promoter Strength", class: "block text-sm font-medium text-gray-700 mb-1" %>
          <%= form.select :strength,
              options_for_select([
                ["", ""],
                ["Weak", "Weak"],
                ["Medium", "Medium"],
                ["Strong", "Strong"],
                ["Very Strong", "Very Strong"]
              ], @promoter.strength),
              { prompt: "Select strength..." },
              { class: "block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm" } %>
        </div>

        <% if @promoter.vectors.any? %>
          <div class="bg-blue-50 border border-blue-200 rounded-md p-4 mb-6">
            <h6 class="text-sm font-medium text-blue-900 mb-2">This promoter is used by <%= @promoter.vectors.count %> vector(s):</h6>
            <ul class="list-disc list-inside text-sm text-blue-700">
              <% @promoter.vectors.limit(5).each do |vector| %>
                <li><%= vector.name %></li>
              <% end %>
              <% if @promoter.vectors.count > 5 %>
                <li>...and <%= @promoter.vectors.count - 5 %> more</li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <div class="flex justify-between items-center">
          <%= link_to "Cancel", admin_promoters_path, class: "inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
          <%= form.submit "Update Promoter", class: "inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
        </div>
      <% end %>
    </div>
  </div>
</div>