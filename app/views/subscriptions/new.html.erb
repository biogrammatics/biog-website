<div class="container mt-4">
  <div class="row">
    <div class="col-md-8 mx-auto">
      <div class="card">
        <div class="card-header">
          <h3>Start Your Twist Vector Subscription</h3>
        </div>
        
        <div class="card-body">
          <%= form_with model: @subscription, local: true do |f| %>
            <% if @subscription.errors.any? %>
              <div class="alert alert-danger" role="alert">
                <h6>Please fix the following errors:</h6>
                <ul class="mb-0">
                  <% @subscription.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                  <% end %>
                </ul>
              </div>
            <% end %>

            <div class="mb-4">
              <h5>Subscription Details</h5>
              <div class="mb-3">
                <%= f.label :twist_username, "Twist Bioscience Username", class: "form-label" %>
                <%= f.text_field :twist_username, class: "form-control", placeholder: "Your Twist account username" %>
                <div class="form-text">
                  We'll use this to link your subscription with your Twist Bioscience account for seamless DNA synthesis.
                </div>
              </div>
              
              <div class="row">
                <div class="col-md-6">
                  <div class="mb-3">
                    <%= f.label :onboarding_fee, "Onboarding Fee", class: "form-label" %>
                    <div class="input-group">
                      <span class="input-group-text">$</span>
                      <%= f.number_field :onboarding_fee, value: 300, step: 0.01, class: "form-control", readonly: true %>
                    </div>
                    <div class="form-text">One-time setup fee for Twist integration</div>
                  </div>
                </div>
                
                <div class="col-md-6">
                  <div class="mb-3">
                    <%= f.label :minimum_prorated_fee, "Minimum Prorated Fee", class: "form-label" %>
                    <div class="input-group">
                      <span class="input-group-text">$</span>
                      <%= f.number_field :minimum_prorated_fee, value: 50, step: 0.01, class: "form-control", readonly: true %>
                    </div>
                    <div class="form-text">Minimum fee for vectors added in last 30 days</div>
                  </div>
                </div>
              </div>
            </div>

            <div class="mb-4">
              <h5>Select Initial Vectors</h5>
              <p class="text-muted">Choose vectors to include in your initial subscription. You can add more vectors later with prorated pricing.</p>
              
              <% if @vectors.any? %>
                <div class="row">
                  <% @vectors.each do |vector| %>
                    <div class="col-md-6 mb-3">
                      <div class="card h-100">
                        <div class="card-body">
                          <div class="form-check">
                            <%= check_box_tag "vector_ids[]", vector.id, params[:vector_id] == vector.id.to_s, 
                                class: "form-check-input", 
                                id: "vector_#{vector.id}" %>
                            <%= label_tag "vector_#{vector.id}", class: "form-check-label w-100" do %>
                              <h6><%= vector.name %></h6>
                              <p class="small mb-2"><%= truncate(vector.description, length: 80) %></p>
                              <div class="d-flex justify-content-between align-items-center">
                                <strong class="text-primary">$<%= vector.subscription_price %>/year</strong>
                                <%= link_to "View Details", vector_path(vector), class: "btn btn-sm btn-outline-secondary", target: "_blank" %>
                              </div>
                            <% end %>
                          </div>
                        </div>
                      </div>
                    </div>
                  <% end %>
                </div>
              <% else %>
                <div class="alert alert-warning" role="alert">
                  No subscription vectors are currently available. You can still create your subscription and add vectors later.
                </div>
              <% end %>
            </div>

            <div class="alert alert-info" role="alert">
              <h6>What happens next?</h6>
              <ol class="mb-0">
                <li>Submit this form to create your subscription</li>
                <li>Pay the onboarding fee and any initial vector subscriptions</li>
                <li>We'll set up your Twist integration</li>
                <li>Your annual subscription will begin immediately</li>
                <li>Add more vectors anytime with prorated pricing</li>
              </ol>
            </div>

            <div class="d-flex justify-content-between mt-4">
              <%= link_to "Cancel", subscriptions_path, class: "btn btn-outline-secondary" %>
              <%= f.submit "Create Subscription", class: "btn btn-primary btn-lg" %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
