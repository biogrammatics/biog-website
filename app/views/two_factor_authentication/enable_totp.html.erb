<div class="max-w-2xl mx-auto px-4 py-8">
  <h1 class="text-3xl font-bold mb-8">Set Up Authenticator App</h1>

  <div class="bg-white rounded-lg shadow-md p-6">
    <div class="space-y-6">
      <div>
        <h2 class="text-xl font-semibold mb-4">Step 1: Install an authenticator app</h2>
        <p class="text-gray-600 mb-4">If you don't have one already, install one of these apps:</p>
        <ul class="list-disc list-inside text-gray-600 space-y-1">
          <li>Google Authenticator (iOS/Android)</li>
          <li>Microsoft Authenticator (iOS/Android)</li>
          <li>Authy (iOS/Android/Desktop)</li>
          <li>1Password (iOS/Android/Desktop)</li>
        </ul>
      </div>

      <div>
        <h2 class="text-xl font-semibold mb-4">Step 2: Scan this QR code</h2>
        <p class="text-gray-600 mb-4">Open your authenticator app and scan this QR code:</p>

        <div class="flex justify-center p-4 bg-gray-50 rounded">
          <%= @qr_code %>
        </div>

        <details class="mt-4">
          <summary class="cursor-pointer text-blue-600 hover:underline">Can't scan? Enter code manually</summary>
          <div class="mt-2 p-4 bg-gray-50 rounded">
            <p class="text-sm text-gray-600 mb-2">Enter this secret key in your authenticator app:</p>
            <code class="block p-2 bg-white border rounded font-mono text-sm break-all"><%= @secret %></code>
          </div>
        </details>
      </div>

      <div>
        <h2 class="text-xl font-semibold mb-4">Step 3: Enter verification code</h2>
        <p class="text-gray-600 mb-4">Enter the 6-digit code from your authenticator app:</p>

        <%= form_with url: confirm_totp_path, local: true do |f| %>
          <% if flash[:alert] %>
            <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded mb-4">
              <%= flash[:alert] %>
            </div>
          <% end %>

          <div class="mb-4">
            <%= f.text_field :otp_code,
                            placeholder: "000000",
                            maxlength: 6,
                            pattern: "[0-9]{6}",
                            inputmode: "numeric",
                            autocomplete: "one-time-code",
                            class: "w-full px-4 py-2 text-2xl text-center tracking-widest border rounded focus:outline-none focus:border-blue-500",
                            required: true,
                            autofocus: true %>
          </div>

          <div class="flex justify-between items-center">
            <%= f.submit "Verify and Enable", class: "bg-blue-600 text-white px-6 py-3 rounded hover:bg-blue-700 transition cursor-pointer" %>
            <%= link_to "Cancel", setup_path, class: "text-gray-600 hover:underline" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>