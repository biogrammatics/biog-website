<div class="max-w-4xl mx-auto px-4 py-8">
  <div class="bg-white rounded-lg shadow-md">
    <div class="bg-gray-50 px-6 py-4 border-b border-gray-200 rounded-t-lg">
      <h3 class="text-2xl font-semibold text-gray-900">Start Your Twist Vector Subscription</h3>
    </div>

    <div class="p-6">
      <%= form_with model: @subscription, local: true do |f| %>
        <% if @subscription.errors.any? %>
          <div class="bg-red-50 border border-red-200 text-red-800 rounded-lg p-4 mb-6" role="alert">
            <h6 class="font-semibold mb-2">Please fix the following errors:</h6>
            <ul class="list-disc list-inside mb-0">
              <% @subscription.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <div class="mb-8">
          <h5 class="text-xl font-semibold text-gray-900 mb-4">Subscription Details</h5>
          <div class="mb-6">
            <%= f.label :twist_username, "Twist Bioscience Username", class: "block text-sm font-medium text-gray-700 mb-2" %>
            <%= f.text_field :twist_username, class: "w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-bio-green focus:border-bio-green transition-colors", placeholder: "Your Twist account username" %>
            <div class="mt-2 text-sm text-gray-600">
              We'll use this to link your subscription with your Twist Bioscience account for seamless DNA synthesis.
            </div>
          </div>

          <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div>
              <div class="mb-6">
                <%= f.label :onboarding_fee, "Onboarding Fee", class: "block text-sm font-medium text-gray-700 mb-2" %>
                <div class="flex">
                  <span class="inline-flex items-center px-3 rounded-l-lg border border-r-0 border-gray-300 bg-gray-50 text-gray-700 text-sm">$</span>
                  <%= f.number_field :onboarding_fee, value: 300, step: 0.01, class: "flex-1 px-4 py-2 border border-gray-300 rounded-r-lg focus:ring-2 focus:ring-bio-green focus:border-bio-green transition-colors", readonly: true %>
                </div>
                <div class="mt-2 text-sm text-gray-600">One-time setup fee for Twist integration</div>
              </div>
            </div>

            <div>
              <div class="mb-6">
                <%= f.label :minimum_prorated_fee, "Minimum Prorated Fee", class: "block text-sm font-medium text-gray-700 mb-2" %>
                <div class="flex">
                  <span class="inline-flex items-center px-3 rounded-l-lg border border-r-0 border-gray-300 bg-gray-50 text-gray-700 text-sm">$</span>
                  <%= f.number_field :minimum_prorated_fee, value: 50, step: 0.01, class: "flex-1 px-4 py-2 border border-gray-300 rounded-r-lg focus:ring-2 focus:ring-bio-green focus:border-bio-green transition-colors", readonly: true %>
                </div>
                <div class="mt-2 text-sm text-gray-600">Minimum fee for vectors added in last 30 days</div>
              </div>
            </div>
          </div>
        </div>

        <div class="mb-8">
          <h5 class="text-xl font-semibold text-gray-900 mb-4">Select Initial Vectors</h5>
          <p class="text-gray-600 mb-4">Choose vectors to include in your initial subscription. You can add more vectors later with prorated pricing.</p>

          <% if @vectors.any? %>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
              <% @vectors.each do |vector| %>
                <div class="mb-6">
                  <div class="bg-white border border-gray-200 rounded-lg shadow-sm h-full">
                    <div class="p-6">
                      <div class="flex items-start">
                        <%= check_box_tag "vector_ids[]", vector.id, params[:vector_id] == vector.id.to_s, class: "mt-1 h-4 w-4 text-bio-green border-gray-300 rounded focus:ring-bio-green focus:ring-2", id: "vector_#{vector.id}" %>
                        <%= label_tag "vector_#{vector.id}", class: "ml-3 flex-1 cursor-pointer" do %>
                          <h6 class="text-lg font-semibold text-gray-900 mb-2"><%= vector.name %></h6>
                          <p class="text-sm text-gray-600 mb-4"><%= truncate(vector.description, length: 80) %></p>
                          <div class="flex justify-between items-center">
                            <strong class="text-bio-green text-lg">$<%= vector.subscription_price %>/year</strong>
                            <%= link_to "View Details", vector_path(vector), class: "px-3 py-1 text-sm text-gray-700 border border-gray-300 rounded-lg hover:bg-gray-50 transition-colors", target: "_blank" %>
                          </div>
                        <% end %>
                      </div>
                    </div>
                  </div>
                </div>
              <% end %>
            </div>
          <% else %>
            <div class="bg-yellow-50 border border-yellow-200 text-yellow-800 rounded-lg p-4" role="alert">
              No subscription vectors are currently available. You can still create your subscription and add vectors later.
            </div>
          <% end %>
        </div>

        <div class="bg-blue-50 border border-blue-200 text-blue-800 rounded-lg p-4 mb-6" role="alert">
          <h6 class="font-semibold mb-2">What happens next?</h6>
          <ol class="list-decimal list-inside mb-0 space-y-1">
            <li>Submit this form to create your subscription</li>
            <li>Pay the onboarding fee and any initial vector subscriptions</li>
            <li>We'll set up your Twist integration</li>
            <li>Your annual subscription will begin immediately</li>
            <li>Add more vectors anytime with prorated pricing</li>
          </ol>
        </div>

        <div class="flex justify-between items-center mt-8">
          <%= link_to "Cancel", subscriptions_path, class: "px-4 py-2 text-gray-700 border border-gray-300 rounded-lg hover:bg-gray-50 transition-colors" %>
          <%= f.submit "Create Subscription", class: "px-6 py-3 bg-bio-green text-white font-semibold rounded-lg hover:bg-green-700 transition-colors text-lg" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
