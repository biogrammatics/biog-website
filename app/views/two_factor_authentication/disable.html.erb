<div class="max-w-2xl mx-auto px-4 py-8">
  <h1 class="text-3xl font-bold mb-8">Disable Two-Factor Authentication</h1>

  <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded mb-6">
    <strong>Warning:</strong> Disabling two-factor authentication will make your account less secure.
    You'll only need your password to sign in.
  </div>

  <div class="bg-white rounded-lg shadow-md p-6">
    <p class="text-gray-700 mb-6">
      To disable two-factor authentication, please confirm your password:
    </p>

    <%= form_with url: two_factor_confirm_disable_path, local: true do |f| %>
      <% if flash[:alert] %>
        <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded mb-4">
          <%= flash[:alert] %>
        </div>
      <% end %>

      <div class="mb-6">
        <%= f.label :password, "Current Password", class: "block text-sm font-medium text-gray-700 mb-2" %>
        <%= f.password_field :password,
                           class: "w-full px-4 py-2 border rounded focus:outline-none focus:border-blue-500",
                           required: true,
                           autofocus: true %>
      </div>

      <div class="bg-gray-50 p-4 rounded mb-6">
        <p class="text-sm text-gray-600 mb-2">After disabling two-factor authentication:</p>
        <ul class="text-sm text-gray-600 space-y-1">
          <li>• Your backup codes will be deleted</li>
          <li>• You'll only need your password to sign in</li>
          <li>• Your account will be less secure</li>
          <% if @user.otp_delivery_method == 'sms' %>
            <li>• We'll send a notification to your phone</li>
          <% end %>
        </ul>
      </div>

      <div class="flex justify-between items-center">
        <%= f.submit "Disable Two-Factor Authentication",
                    class: "bg-red-600 text-white px-6 py-3 rounded hover:bg-red-700 transition cursor-pointer",
                    data: { confirm: "Are you sure you want to disable two-factor authentication?" } %>
        <%= link_to "Cancel", two_factor_setup_path, class: "text-gray-600 hover:underline" %>
      </div>
    <% end %>
  </div>
</div>